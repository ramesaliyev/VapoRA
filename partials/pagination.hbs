<nav class="pagination" role="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{pageUrl prev}}"><i class="fa fa-chevron-circle-left"></i> Yeniler</a>
    {{/if}}
    <span class="page-number">Sayfa {{page}} / {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{pageUrl next}}">Eskiler <i class="fa fa-chevron-circle-right"></i></a>
    {{/if}}
</nav>